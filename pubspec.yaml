name: huobi_websocket
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  web_socket_channel: ^2.4.0
  archive: ^3.1.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
